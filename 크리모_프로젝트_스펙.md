# 크리모(Creamo) 프로젝트 스펙

크리에이터 공동구매(셀렉트 커머스) 플랫폼. 크리에이터가 상품을 추천하고 커미션을 받는 구조이며,
벤더는 상품·재고를 공급하고, 구매자는 플랫폼 내에서 회원가입부터 결제까지 완료한다.
한국 졸업작품(캡스톤) 프로젝트이며 실제 개발 기간은 4주다.

## 1. 프로젝트 개요

- 서비스명: 크리모 (Creamo)
- 형태: 반응형 웹 (모바일 앱 없음, 처음부터 확장 가능하게 설계)
- 결제: 즉시결제형, 플랫폼 내 결제(카드/카카오페이/네이버페이/무통장입금). 결제 인증 과정에서 팝업이나
  결제사 페이지로 이동하는 것은 허용하되, 외부의 별도 쇼핑몰로 이탈하는 구조는 만들지 않는다.
- 개발 기간: 4주 (1주 기획 확정·설계 / 2주 회원·상품 UI / 3주 결제·대시보드·정산·관리자 기능 / 4주 통합 테스트)

## 2. 사용자 역할

시스템 로그인 계정은 **구매자 / 크리에이터 / 관리자** 3종류다. 벤더는 시스템 로그인 계정이 없다.

### 2.1 구매자
회원가입·로그인 → 메인(크리에이터 카드형 목록) 또는 카테고리 목록 → 상품 상세 조회(추천 크리에이터·공급
벤더 표시) → 옵션/수량 선택 후 장바구니 담기 또는 바로구매 → 결제 → 주문 내역 및 배송 상태 조회.
한 주문에 여러 크리에이터·벤더의 상품이 섞여 담길 수 있으며, 각 상품(OrderItem)마다 추천 크리에이터와
공급 벤더 정보를 개별로 가진다.

### 2.2 크리에이터
회원가입·프로필 작성(핸들/카테고리/소개) → **관리자 승인 대기** → 승인 후 상품 카탈로그에서 추천하고
싶은 상품을 선택해 "내 추천 목록"에 등록(커미션율 확인) → 추천 링크 공유 → 대시보드에서 클릭수·판매수·
누적 커미션·정산 상태/이력 확인.
승인 전에도 로그인은 되지만 "심사 중" 화면만 노출되고 추천 등록·대시보드는 사용할 수 없다.

### 2.3 벤더 (시스템 로그인 없음)
오프라인(이메일 등)으로 상품 정보(이름/가격/옵션/재고/이미지)와 사업자 정보를 관리자에게 전달한다.
플랫폼 화면에는 상품 상세의 "공급 벤더" 배지로만 노출되며, 정산은 관리자가 정산 승인 화면에서 벤더 몫을
확인해 처리한다.

### 2.4 관리자 (운영진 내부 계정)
로그인 → 신규 신청 승인 대기 목록(벤더+크리에이터 통합, 구분 표시)에서 승인/반려 → 벤더 상품 등록·수정
(벤더로부터 받은 정보 대리 입력) → 주문 배송 상태 변경(상품준비/배송중/배송완료) → 정산 승인 대기 목록에서
벤더·크리에이터 정산 건 승인 → 회원 관리.

## 3. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 백엔드 | Django + Django REST Framework | 관계형 데이터·회원 권한 관리에 강함 |
| 프론트엔드 | Next.js (TypeScript) | 반응형 웹 |
| DB | PostgreSQL | 상품 옵션 등 가변 필드는 JSONB 컬럼 사용 |
| 비동기 처리 | Celery + Redis | 정산 배치 계산, PG 웹훅 처리 등 즉시 응답이 필요 없는 작업 |
| 결제 | PortOne(PG) | 벤더/크리에이터 분리 정산(멀티벤더 정산) 지원 서비스 우선 검토 |
| 배포 | Docker + AWS | 초기 단일 서버, 이후 컨테이너 스케일아웃으로 확장 가능하게 |

## 4. 데이터 모델

| 엔티티 | 주요 필드 |
|---|---|
| User | id, email, password_hash, name, role(buyer/creator/admin), created_at |
| CreatorProfile | user_id(FK), handle, category, intro, profile_image, status(승인대기/승인/반려), applied_at, approved_at |
| VendorProfile | name, business_no, contact, settlement_account, status(승인대기/승인/반려) — User와 연결 없음, 관리자 전용 마스터 데이터 |
| Category | id, name, parent_id(nullable) |
| Product | id, vendor_id(FK), category_id(FK), name, description, price, thumbnail, options(JSONB), stock, status(판매중/품절/비활성) |
| CreatorRecommendation | creator_id(FK), product_id(FK), commission_rate, created_at |
| Order | id, buyer_id(FK), total_amount, created_at |
| OrderItem | id, order_id(FK), product_id(FK), creator_id(FK, nullable), quantity, unit_price, commission_amount, status(결제완료/상품준비/배송중/배송완료) |
| Payment | order_id(FK), pg_transaction_id, method, status, paid_at |
| Settlement | target_type(vendor/creator), target_id, amount, period_start, period_end, status(대기/승인/완료), approved_at |

## 5. API 명세

| 구분 | Method | Path | 설명 |
|---|---|---|---|
| 인증 | POST | /auth/signup | 회원가입 (buyer/creator 선택) |
| 인증 | POST | /auth/login | 로그인 |
| 인증 | POST | /auth/logout | 로그아웃 |
| 인증 | GET | /auth/me | 내 정보 조회 |
| 상품 | GET | /products | 상품 목록 (카테고리/크리에이터 필터) |
| 상품 | GET | /products/{id} | 상품 상세 |
| 카테고리 | GET | /categories | 카테고리 목록 |
| 크리에이터 | GET | /creators | 크리에이터 목록 |
| 크리에이터 | GET | /creators/{id} | 크리에이터 프로필 |
| 크리에이터 | GET | /creators/{id}/products | 크리에이터 추천 상품 목록 |
| 크리에이터 | POST | /creator/recommendations | 추천 상품 등록 (승인된 크리에이터만) |
| 장바구니 | GET/POST | /cart | 장바구니 조회/담기 |
| 주문 | POST | /orders | 결제 요청(주문 생성) |
| 주문 | GET | /orders | 내 주문 목록 |
| 주문 | GET | /orders/{id} | 주문 상세 (아이템별 상태 포함) |
| 결제 | POST | /payments/request | 결제 요청 |
| 결제 | POST | /payments/webhook | PG 콜백 수신 |
| 크리에이터 대시보드 | GET | /creator/dashboard/stats | 클릭수/판매수/커미션 통계 |
| 크리에이터 대시보드 | GET | /creator/dashboard/products | 추천 상품별 성과 |
| 관리자 | GET/POST | /admin/applications | 벤더·크리에이터 신청 심사 (승인/반려) |
| 관리자 | GET/POST | /admin/products | 벤더 상품 대리 등록/수정 |
| 관리자 | PATCH | /admin/order-items/{id}/status | 배송 상태 변경 |
| 관리자 | GET/POST | /admin/settlements | 정산 승인 |

## 6. 폴더 구조 (예정)

```
creamo/
├── backend/                  # Django + DRF
│   ├── config/                # settings, urls, wsgi
│   ├── accounts/               # User, CreatorProfile
│   ├── vendors/                 # VendorProfile
│   ├── catalog/                  # Category, Product
│   ├── recommendations/           # CreatorRecommendation
│   ├── orders/                     # Order, OrderItem
│   ├── payments/                    # Payment, PG 연동
│   ├── settlements/                  # Settlement, Celery tasks
│   ├── adminconsole/                  # 관리자 전용 API
│   └── manage.py
├── frontend/                  # Next.js (TypeScript)
│   ├── app/
│   ├── components/
│   └── lib/
├── docker-compose.yml           # PostgreSQL, Redis
└── README.md
```

## 7. 프론트엔드 구현 방향

- 구조: Next.js(App Router) + TypeScript. 스타일은 Tailwind CSS를 사용한다(기존에 만든 화면 목업의
  유틸리티 클래스 조합 형태라 그대로 옮기기 편하다).
- 페이지 구성 (화면 목업과 1:1 매핑): `/`(메인, 크리에이터 카드형 목록), `/category`(카테고리 목록),
  `/products/[id]`(상품 상세·결제), `/cart`(장바구니), `/creator/dashboard`(크리에이터 대시보드),
  `/admin`(관리자 콘솔).
- 공통 컴포넌트: `Header`(내비/검색/장바구니), `CreatorCard`, `ProductCard`, `StatusTag`(승인대기·배송중
  등 상태 배지), `StatCard`(대시보드 KPI 카드), `DataTable`(관리자 목록용). 화면 5개 대부분이 이 컴포넌트
  조합으로 재사용 가능하다.
- 개발 순서: 2주차에는 API 연동 없이 목업 데이터로 정적 UI부터 작성하고, 3주차에 백엔드 API가 준비되면
  fetch를 붙이는 순서로 진행한다. 프론트·백엔드를 동시에 왔다갔다 하는 것보다 UI를 먼저 굳히는 편이
  4주 일정에 적합하다.
- 상태 관리: 이 규모에서는 Redux 같은 별도 라이브러리 없이, 장바구니만 React Context나 로컬스토리지로
  처리한다. 크리에이터 대시보드·관리자 목록 등의 API 응답은 컴포넌트 로컬 state로 두고 있는 것으로
  충분하다.
- API 연동: 백엔드 base URL을 환경변수(`.env.local`)로 분리하고, `fetch`를 감싼 얇은 유틸 함수 하나만
  만들어 사용한다(axios 등 별도 라이브러리 추가 불필요).

## 8. 개발 일정 (4주)

| 주차 | 내용 |
|---|---|
| 1주차 | 기획 확정 · 데이터 모델/API 설계 |
| 2주차 | 회원·크리에이터 승인(승인 포함), 카드형/카테고리 화면 UI(정적 UI) |
| 3주차 | 장바구니·결제(PG) 연동, 크리에이터 추천 성과 대시보드, 정산 로직, 관리자 기능(벤더 상품 등록/수정, 신청 심사, 정산 승인), 프론트-백엔드 API 연동 |
| 4주차 | 통합 테스트 · 발표 준비 |

## 9. 향후 확장 계획 (이번 스코프 제외)

- 배송 실시간 추적 (택배사 API 연동, 송장번호 조회) → 이번엔 상태값(결제완료/상품준비/배송중/배송완료)
  관리까지만 하고, 실제 위치·진행률 조회는 다음 단계로 미룬다.
- 벤더 자체 로그인/대시보드 (현재는 관리자 대행 등록)

## 10. 코딩 컨벤션

- Python(Django) 코드는 명시적이고 읽기 쉬운 스타일로 작성한다: f-string, list comprehension, `with` 문
  축약, 삼항 연산자, `enumerate` 같은 파이썬 특유의 축약 문법 대신 풀어 이 코드를 사용한다.
- 요청받지 않은 리팩터링은 하지 않는다.
- 새 파일을 만들거나 큰 변경을 하기 전엔 먼저 계획/이슈를 보여주고 확인받는다.
